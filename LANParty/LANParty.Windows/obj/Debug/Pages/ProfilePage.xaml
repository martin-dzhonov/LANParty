<Page
    x:Name="pageRoot"
    x:Class="LANParty.Pages.ProfilePage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LANParty.Pages"
    xmlns:common="using:LANParty.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Name="profilePic" 
                       Height="100" 
                       Width="100" 
                       HorizontalAlignment="Left" 
                       Source="{Binding ProfilePic, Converter={StaticResource ParseFileToBitmapImageConverter}}"></Image>
                    <StackPanel>
                    <TextBlock FontSize="25"
                           Name="username"
                           Text="{Binding Username, Mode=TwoWay}" ></TextBlock>
                        <Button x:ConnectionId='5' Content="Edit Profile"                           ></Button>

                    </StackPanel>
                </StackPanel>
                <Button x:ConnectionId='1' Content="Create Party"                     ></Button>
                <Button x:ConnectionId='2' Content="Search for party"                       ></Button>
                <Button x:ConnectionId='3' Content="Created parties"                       ></Button>
                <Button x:ConnectionId='4' Content="Joined parties"                       ></Button>
            </StackPanel>
        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar IsOpen="True" IsSticky="True">
            <AppBarButton Label="Logout" Icon="ClosePane" Command="{Binding Logout}"/>
            <AppBarButton x:ConnectionId='6' Label="Inbox" Icon="Mail"                       />
            <AppBarButton Label="Friend List" Icon="People"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>

